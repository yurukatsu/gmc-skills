# Dependencies
node_modules/

# Build outputs
dist/
*.tsbuildinfo

# Environment files (contain secrets — never commit)
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Runtime storage (server)
registry-storage/
*.gmc-stats.json

# CLI local config (do not commit credentials)
.gmc-skills/

# Claude Code settings (per-machine; populate a clean shared settings.json later if needed)
.claude/settings.json
.claude/settings.local.json

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
